# now springboot application will pick one random port for this application
server.port = 0
spring.datasource.url = jdbc:mysql://localhost:3306/inventoryservice?createDatabaseIfNotExists=true&useSSL=true
spring.datasource.username = budcoded
spring.datasource.password = 29MNsgjdavkrk35#22av12
spring.jpa.hibernate.ddl-auto = create-drop
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka
spring.application.name=inventory-service
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}